# Bit-banged 9-bit SPI — the panel needs plain GPIOs, not a SPI peripheral.
CONFIG_GPIO=y

# Nothing here prints; drop logging and the console to keep the image minimal.
CONFIG_LOG=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_SERIAL=n
